#include<iostream>
using namespace std;
void RotateArray(int arr[],int n,int key)]

{

      int count=0;
      for(int i=0;i<n;i++)
      {
        for(int i=0;i<candles.size();i++)
        {
            if(candles[i]==key)
            {
                  count++;
            }
        }
        return count;
      }
}
int main()
{
 int arr[5]={3,5,2,1,6};
 RotateArray(arr,5,2);

    return 0;
}